{
  "name": "lostark",
  "version": "1.0.0",
  "description": "Lost Ark KOR server queue checking site",
  "author": "RubystarAshe",
  "private": true,
  "scripts": {
    "dev": "nuxt",
    "build": "nuxt build",
    "start": "nuxt start",
    "generate": "nuxt generate",
    "deploy": "push-dir --dir=dist --branch=gh-pages --cleanup",
    "publish": "npm run generate && npm run deploy"
  },
  "dependencies": {
    "cross-env": "^5.2.0",
    "element-ui": "^2.4.6",
    "firebase": "^5.5.8",
    "nuxt": "^2.3.1",
    "vuexfire": "^2.3.1"
  },
  "devDependencies": {
    "node-sass": "^4.10.0",
    "nodemon": "^1.11.0",
    "postcss-loader": "^3.0.0",
    "sass-loader": "^7.1.0"
  }
}
